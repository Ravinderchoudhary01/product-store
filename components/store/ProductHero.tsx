import type {Product} from '@/types/product';
import Image from 'next/image';
import BuyButton from './BuyButton';
export default function ProductHero({product}:{product:Product}){return <div className="product-layout"><div><div className="eyebrow"><span className="eyebrow-dot"/> Digital product</div><h1 className="product-page-title">{product.title}</h1><p className="lead">{product.short_description}</p><div className="product-purchase"><span className="price-large">₹{Number(product.price).toFixed(0)}</span><BuyButton productId={product.id}/></div><div className="tiny-trust"><span>✓ One-time payment</span><span>✓ Instant access</span><span>✓ Secure checkout</span></div></div><div className="product-cover-large">{product.thumbnail_url?<Image src={product.thumbnail_url} alt={product.title}/>:<div className="mock-book"><span className="sun"/><span className="book-kicker">Digital product</span><strong className="book-title">{product.title}</strong><span className="book-bottom">A practical resource, ready when you are.</span></div>}</div></div>}