import type {Product} from '@/types/product';
import Image from 'next/image';
export default function ProductCard({product}:{product:Product}){return <article className="product-card"><a href={`/products/${product.slug}`}><div className="product-cover">{product.thumbnail_url?<img src={product.thumbnail_url} alt={product.title} />:<div className="cover-placeholder"><small>DIGITAL STORE</small><strong>{product.title}</strong><small>Instant digital access</small></div>}</div></a><div className="product-info"><h3>{product.title}</h3><p>{product.short_description}</p><div className="product-meta"><span className="product-price">₹{Number(product.price).toFixed(0)}</span><a className="btn secondary small-btn" href={`/products/${product.slug}`}>View product <span>↗</span></a></div></div></article>}
