'use client';export default function DeleteProductModal({id}:{id:string}){const del=async()=>{if(!confirm('Delete this product? Existing orders are retained.'))return;const r=await fetch(`/api/admin/products/${id}`,{method:'DELETE'});if(r.ok)location.reload();else alert((await r.json()).error||'Delete failed')};return <button className="btn danger small-btn" onClick={del}>Delete</button>}
