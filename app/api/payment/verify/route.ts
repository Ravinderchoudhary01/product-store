import crypto from 'crypto';import {NextResponse} from 'next/server';import {createAdminClient} from '@/lib/supabase/admin';import {randomToken} from '@/lib/utils';import {z} from 'zod';
export async function POST(req:Request){try{const body=z.object({razorpay_order_id:z.string(),razorpay_payment_id:z.string(),razorpay_signature:z.string(),orderId:z.string().uuid()}).parse(await req.json());const expected=crypto.createHmac('sha256',process.env.RAZORPAY_KEY_SECRET!).update(`${body.razorpay_order_id}|${body.razorpay_payment_id}`).digest('hex');if(expected!==body.razorpay_signature)return NextResponse.json({error:'Invalid payment signature'},{status:400});const db=createAdminClient();const {data:order}=await db.from('orders').select('*,products(file_path)').eq('id',body.orderId).eq('razorpay_order_id',body.razorpay_order_id).single();if(!order)return NextResponse.json({error:'Order not found'},{status:404});await db.from('orders').update({razorpay_payment_id:body.razorpay_payment_id,payment_status:'paid'}).eq('id',order.id);const token=randomToken();const mins=Number(process.env.DOWNLOAD_TOKEN_TTL_MINUTES||30);await db.from('download_tokens').insert({order_id:order.id,token,expires_at:new Date(Date.now()+mins*60000).toISOString()});return NextResponse.json({redirect:`/payment/success?token=${token}`})}catch(e:any){return NextResponse.json({error:e.message||'Verification failed'},{status:400})}}
