import AdminHeader from '@/components/admin/AdminHeader';import ProductForm from '@/components/admin/ProductForm';import {createAdminClient} from '@/lib/supabase/admin';import {notFound} from 'next/navigation';export default async function Edit({params}:{params:Promise<{id:string}>}){const {id}=await params;const {data}=await createAdminClient().from('products').select('*').eq('id',id).single();if(!data)notFound();return <><AdminHeader title="Edit product"/><ProductForm initial={data}/></>}
