import AdminHeader from '@/components/admin/AdminHeader';import {createAdminClient} from '@/lib/supabase/admin';export default async function Orders(){const {data}=await createAdminClient().from('orders').select('*,products(title)').order('created_at',{ascending:false});return <><AdminHeader title="Orders"/><div className="admin-card"><div style={{marginBottom:20}}><strong style={{fontFamily:'Plus Jakarta Sans'}}>Customer orders</strong><div className="muted" style={{fontSize:13,marginTop:4}}>Payments and digital product purchases.</div></div><div className="table-wrap"><table className="table"><thead><tr><th>Customer</th><th>Product</th><th>Amount</th><th>Payment</th><th>Date</th></tr></thead><tbody>{data?.map(o=><tr key={o.id}><td><strong>{o.customer_name}</strong><br/><span className="muted">{o.customer_email}</span></td><td>{(o as any).products?.title}</td><td>₹{Number(o.amount).toFixed(0)}</td><td><span className={`badge ${o.payment_status}`}>{o.payment_status}</span></td><td>{new Date(o.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></div></>}
