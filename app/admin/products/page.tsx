import AdminHeader from '@/components/admin/AdminHeader';import DeleteProductModal from '@/components/admin/DeleteProductModal';import {createAdminClient} from '@/lib/supabase/admin';
export default async function Products(){const {data}=await createAdminClient().from('products').select('*').order('created_at',{ascending:false});return <><AdminHeader title="Products"/><div className="admin-card"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:15,marginBottom:20}}><div><strong style={{fontFamily:'Plus Jakarta Sans'}}>Your catalog</strong><div className="muted" style={{fontSize:13,marginTop:4}}>Publish, edit or remove digital products.</div></div><a className="btn warm" href="/admin/products/new">＋ Add product</a></div>{data?.length?<div className="table-wrap"><table className="table"><thead><tr><th>Product</th><th>Price</th><th>Status</th><th>Actions</th></tr></thead><tbody>{data.map(p=><tr key={p.id}><td><strong>{p.title}</strong><br/><span className="muted">/{p.slug}</span></td><td>₹{Number(p.price).toFixed(0)}</td><td><span className={`badge ${p.status}`}>{p.status}</span></td><td><div style={{display:'flex',gap:8}}><a className="btn secondary small-btn" href={`/admin/products/${p.id}/edit`}>Edit</a><DeleteProductModal id={p.id}/></div></td></tr>)}</tbody></table></div>:<div className="empty-state">No products yet. Add your first one above.</div>}</div></>}
